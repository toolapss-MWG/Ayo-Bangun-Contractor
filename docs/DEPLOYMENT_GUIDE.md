# Deployment Guide

GitHub:
git init
git add .
git commit -m "AyoBangun Contractor v1"

Firebase:
firebase login
firebase init
firebase deploy

Android:
npx cap add android
npx cap sync
npx cap open android